import { db, fail } from "../../_lib/db";
import { requireAdmin } from "../../_lib/auth";
export async function GET(request: Request) { const auth = await requireAdmin(request); if (auth.error) return auth.error; return Response.json(await db.menuItem.findMany({ orderBy: { id: "asc" } })); }
export async function POST(request: Request) { const auth = await requireAdmin(request); if (auth.error) return auth.error; try { const body = await request.json(); return Response.json(await db.menuItem.create({ data: { id: body.id || `menu-${Date.now()}`, name: body.name, description: body.description || "", price: Number(body.price), category: body.category, dietary: body.dietary || [], image: body.image || "https://images.unsplash.com/photo-1546069901-ba9599a7e63c?auto=format&fit=crop&q=80&w=1200", isSignature: Boolean(body.isSignature), pairingNote: body.pairingNote || null, available: body.available !== false } }), { status: 201 }); } catch { return fail("Could not create menu item"); } }
