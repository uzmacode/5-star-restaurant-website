export function SectionHeading({ eyebrow, title, children }: { eyebrow: string; title: string; children?: React.ReactNode }) { return <header className="text-center max-w-3xl mx-auto mb-14 space-y-3"><span className="font-mono-price text-xs uppercase tracking-[.25em] text-[var(--flame)] font-semibold">{eyebrow}</span><h2 className="font-display text-3xl sm:text-4xl md:text-5xl">{title} {children}</h2></header>; }
