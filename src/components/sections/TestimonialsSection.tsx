"use client";
import { useState } from "react"; import { ChevronLeft, ChevronRight, Quote, Star } from "lucide-react"; import { TESTIMONIALS } from "@/data/restaurantData"; import { SectionHeading } from "@/components/ui/SectionHeading";
export function TestimonialsSection(){const [index,setIndex]=useState(0);const current=TESTIMONIALS[index];return <section id="testimonials" className="py-24"><div className="max-w-3xl mx-auto px-6"><SectionHeading eyebrow="CRITICAL ACCLAIM & PRAISE" title="Words from Our Guests"/><div className="sketch comic-shadow bg-[var(--card)] p-8 sm:p-12"><Quote className="text-[var(--flame)] mb-4"/><div className="flex">{Array.from({length:current.rating},(_,i)=><Star key={i} className="fill-[var(--butter)] text-[var(--ink)]"/>)}</div><p className="font-display italic text-xl sm:text-2xl my-6">&ldquo;{current.quote}&rdquo;</p><div className="border-t-[3px] border-[var(--ink)] pt-4 flex justify-between"><span className="font-display">{current.author}<small className="block text-xs text-[var(--flame)]">{current.role}</small></span><div className="flex gap-2"><button className="btn-2d p-2 bg-[var(--cream-2)]" onClick={()=>setIndex((index-1+TESTIMONIALS.length)%TESTIMONIALS.length)} aria-label="Previous review"><ChevronLeft/></button><button className="btn-2d p-2 bg-[var(--cream-2)]" onClick={()=>setIndex((index+1)%TESTIMONIALS.length)} aria-label="Next review"><ChevronRight/></button></div></div></div></div></section>}
export default TestimonialsSection;
