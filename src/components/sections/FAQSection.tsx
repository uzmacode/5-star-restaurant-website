"use client";
import { useState } from "react"; import { FAQ_ITEMS } from "@/data/restaurantData"; import { SectionHeading } from "@/components/ui/SectionHeading";
export function FAQSection(){const [open,setOpen]=useState<string|null>(FAQ_ITEMS[0].id);return <section id="faq" className="py-24"><div className="max-w-3xl mx-auto px-6"><SectionHeading eyebrow="CURATED INQUIRIES" title="Frequently Asked Questions"/><p className="text-center font-serif-subtle mb-10">Everything you need to know regarding dining policies, dress code, wine corkage, and private events.</p>{FAQ_ITEMS.map(item=><div key={item.id} data-open={open===item.id} className="faq-panel sketch-alt mb-4"><button onClick={()=>setOpen(open===item.id?null:item.id)} aria-expanded={open===item.id} className="w-full p-5 flex justify-between text-left font-display text-lg"><span>{item.question}</span><span className="faq-plus">{open===item.id?"×":"+"}</span></button>{open===item.id&&<div className="p-5 pt-0 text-sm text-[var(--ink)]/70 border-t-[3px] border-[var(--ink)]"><p>{item.answer}</p><small className="text-[var(--flame)] uppercase">Category: {item.category}</small></div>}</div>)}</div></section>}; export default FAQSection;
