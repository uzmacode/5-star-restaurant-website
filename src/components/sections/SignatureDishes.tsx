"use client";
import Link from "next/link"; import { SIGNATURE_DISHES } from "@/data/restaurantData"; import { DishCard } from "@/components/ui/DishCard"; import { SectionHeading } from "@/components/ui/SectionHeading";
export function SignatureDishes({ limit }: { limit?: number }) { return <section id="signatures" className="py-24"><div className="max-w-7xl mx-auto px-6"><SectionHeading eyebrow="CHEF ARTHUR VANCE’S CURATION" title="Signature Dishes" /><p className="text-center font-serif-subtle text-base max-w-xl mx-auto mb-12">Perennial favorites crafted from morning harvests in our Kent walled garden and pristine coastal catches.</p><div className="grid sm:grid-cols-2 xl:grid-cols-4 gap-8">{SIGNATURE_DISHES.slice(0,limit).map(dish=><DishCard key={dish.id} dish={dish}/>)}</div>{limit&&<div className="text-center mt-10"><Link href="/signatures" className="btn-2d px-5 py-3 bg-[var(--card)] text-xs uppercase tracking-widest">View All Signatures</Link></div>}</div></section>; }
export default SignatureDishes;
