"use client";
import { useCart } from "@/context/CartContext"; import type { OrderDetails } from "@/types";
export function OrdersHistoryModal({isOpen,onClose,onSelectOrder}:{isOpen:boolean;onClose:()=>void;onSelectOrder:(order:OrderDetails)=>void}){const {orders}=useCart();if(!isOpen)return null;return <div className="fixed inset-0 z-50 bg-[var(--cherry)]/80 flex items-center justify-center p-6" onClick={onClose}><div className="bg-[var(--cream)] max-w-xl w-full p-6" onClick={e=>e.stopPropagation()}><button onClick={onClose} className="float-right">Close</button><h2 className="font-display text-2xl">Recent Order Receipts</h2>{orders.length===0?<p className="text-sm py-8">No recent orders found on this device.</p>:orders.map(order=><button key={order.id} onClick={()=>onSelectOrder(order)} className="block w-full text-left border-b py-4">{order.orderNumber} · £{order.total.toFixed(2)}</button>)}</div></div>}
export default OrdersHistoryModal;
